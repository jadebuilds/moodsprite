# Peer Interaction

To discover Ghostwheel's skills: `http_request GET http://ghostwheel:3100/`
To invoke Ghostwheel's skill: `http_request POST http://ghostwheel:3100/skills/report-bug {"summary":"..."}`

To discover Alastair's skills: `http_request GET http://alastair:3300/`
To invoke Alastair's skill: `http_request POST http://alastair:3300/skills/{name} {...}`
